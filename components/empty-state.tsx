import { SearchX, Heart } from "lucide-react";
export function EmptyState({ saved = false, title, message, action }: { saved?: boolean; title: string; message: string; action?: React.ReactNode }) { const Icon = saved ? Heart : SearchX; return <div className="flex min-h-[300px] flex-col items-center justify-center rounded-xl border border-dashed border-[#cfd6d5] bg-white px-5 text-center"><span className="flex h-14 w-14 items-center justify-center rounded-full bg-[#fff0e8] text-[var(--orange)]"><Icon size={25} /></span><h2 className="mt-5 text-xl font-bold text-[var(--navy)]">{title}</h2><p className="mt-2 max-w-sm text-sm leading-6 text-[#697580]">{message}</p>{action && <div className="mt-5">{action}</div>}</div>; }
