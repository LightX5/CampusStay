"use client";
import Link from "next/link";
import { X, LockKeyhole } from "lucide-react";
export function AuthModal({ open, onClose, action }: { open: boolean; onClose: () => void; action: string }) { if (!open) return null; return <div className="fixed inset-0 z-50 flex items-center justify-center bg-[#102a3dcc] p-5" role="dialog" aria-modal="true"><div className="relative w-full max-w-md rounded-xl bg-white p-7 shadow-2xl"><button onClick={onClose} aria-label="Close" className="absolute right-4 top-4 rounded-lg p-2 text-[#697580] hover:bg-[var(--cream)]"><X size={20} /></button><div className="flex h-12 w-12 items-center justify-center rounded-full bg-[#fff0e8] text-[var(--orange)]"><LockKeyhole size={22} /></div><h2 className="mt-5 text-2xl font-bold text-[var(--navy)]">Create an account to {action}</h2><p className="mt-2 text-sm leading-6 text-[#697580]">Save your favourites and keep your accommodation search in one place.</p><div className="mt-6 grid gap-3"><Link href="/signup" onClick={onClose} className="flex min-h-11 items-center justify-center rounded-lg bg-[var(--orange)] text-sm font-bold text-white">Create an account</Link><Link href="/login" onClick={onClose} className="flex min-h-11 items-center justify-center rounded-lg border border-[var(--navy)] text-sm font-bold text-[var(--navy)]">Log in</Link></div></div></div>; }
