import Link from "next/link";
import { ArrowRight, Search, SlidersHorizontal, Send } from "lucide-react";
import { Button } from "@/components/button";
export default function HowItWorksPage() { return <main className="container min-h-[650px] py-16 md:py-24"><div className="max-w-2xl"><p className="eyebrow">A simpler search</p><h1 className="mt-4 text-5xl font-black tracking-[-.06em] text-[var(--navy)]">Find a place that fits your student life.</h1><p className="mt-6 text-lg leading-8 text-[#697580]">CampusStay helps you compare the things that matter, from distance and price to the facilities you actually use every day.</p></div><div className="mt-16 grid gap-5 md:grid-cols-3">{[[Search, "Search nearby", "Start with your campus area, preferred budget or the name of a place you heard about."], [SlidersHorizontal, "Compare clearly", "Use practical filters for type, facilities and distance so you can narrow the list quickly."], [Send, "Take the next step", "Save the homes you like, then contact a landlord when you are ready to arrange a viewing."]].map(([Icon, title, text], index) => <div key={title as string} className="border-t-2 border-[var(--orange)] bg-white p-6"><span className="text-[var(--orange)]"><Icon size={25} /></span><p className="mt-8 text-xs font-bold text-[#9aa3a6]">0{index + 1}</p><h2 className="mt-2 text-xl font-bold text-[var(--navy)]">{title as string}</h2><p className="mt-3 text-sm leading-6 text-[#697580]">{text as string}</p></div>)}</div><Link href="/search"><Button className="mt-12">Start browsing <ArrowRight size={16} /></Button></Link></main>; }
